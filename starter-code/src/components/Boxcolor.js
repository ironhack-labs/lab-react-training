import React from 'react'
import {Boxcolor} from '../styles/styles'

const BoxcolorComp = ({r,g,b}) => {
    // style={`background-color: rgba(${r}, ${g}, ${b})`}
    return (
        <Boxcolor>
        </Boxcolor>
    )
}

export default BoxcolorComp
