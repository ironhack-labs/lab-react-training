import React from 'react';

const Random = props => {
    return (
        <div className = "box">
            Random value between {props.min} and {props.max} => 
        </div>
    )
}

export default Random;