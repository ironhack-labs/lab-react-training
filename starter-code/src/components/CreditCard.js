import React, { Component } from "react";

class CreditCard extends Comment {
  constructor(props) {
    super(props);
  }
  return() {
    if (this.props.type === "Visa") {
      return;
    } else {
    }
  }
}

export default CreditCard;
