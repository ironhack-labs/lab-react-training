import React from 'react';

export default function BoxColor(props) {
	return <div style={{ background: `rgb(${props.r} ,${props.g}, ${props.b})`}
   
    
    
    
    }>

rgb ({props.r} ,{props.g}, {props.b})
    </div>;
}
