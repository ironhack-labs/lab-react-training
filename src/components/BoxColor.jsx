import React from 'react'

function BoxColor({ r, g, b }) {
    return (
        <div className='box'>
            {backgroundColor: 'url(' + imgUrl + ')'}
        </div>
    )
}

export default BoxColor