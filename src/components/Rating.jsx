const Rating = (props) => {

    return (<div>
        <p>{props.children}</p>
        <p>{props.children}</p>
        <p>{props.children}</p>
        <p>{props.children}</p>
        <p>{props.children}</p>
        <p>{props.children}</p>
    </div>

    )


}

export default Rating