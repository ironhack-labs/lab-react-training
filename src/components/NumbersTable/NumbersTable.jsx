import { useState } from 'react'

const NumbersTable = ({ limit }) => {

    const number = []
    for (let i = 0; i < limit; i++);
    number.push();
    return (
        <div>

        </div>
    )
}
export default NumbersTable;